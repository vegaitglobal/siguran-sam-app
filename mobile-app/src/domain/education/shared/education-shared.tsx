import { View } from 'react-native';
import { styles } from './education-shared.style';

export const ListHeader = () => <View style={styles.listHeader} />;

export const ItemSeparator = () => <View style={styles.itemSeparator} />;
